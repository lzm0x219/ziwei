# 仓库指南

## 全局规则

保持变更范围聚焦，保留工作树中与当前任务无关的修改。

## 按需读取

- **Zig 开发**：修改 Zig 源码、公开 API、构建配置或运行验证前，完整读取 `docs/agents/zig-development.md`。
- **领域建模**：探索或修改领域概念、术语、历法规则、分析语义或架构前，完整读取 `docs/agents/domain.md`。
- **工作项跟踪**：读取、创建、更新或关闭工作项时，完整读取 `docs/agents/issue-tracker.md`。
- **分诊**：分诊工作项或使用分诊标签时，完整读取 `docs/agents/triage-labels.md`。
