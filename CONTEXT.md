# Ziwei

MathArts 紫微斗数排盘引擎：从已消解的农历出生资料构造可查询命盘。

## Language

**ZiweiBirth**:
供 `from_birth` 使用的农历出生资料：性别，以及打平的年序号、月位（正月 = 0）、日（初一 = 1）、时辰位（子时 = 0）。不嵌套日期对象；由 `ZiweiBirth::try_new` 在边界校验月日时。
_Avoid_: BirthInfo、NormalizedDate（作入口嵌套）、公历生日、未消解的时钟时刻

**ZiweiInput**:
`from_input` 的原始量捷径：性别、生年干支、月/日/时（零点同 `ZiweiBirth`）。不含真实出生年、命宫、紫微等排盘结果。
_Avoid_: 代表年、命宫位注入、紫微位注入、完整十二宫结果图、出生证明

**from_birth**:
从 `ZiweiBirth` 走权威全管道构造命盘的入口；排盘结果一律引擎算。
_Avoid_: calculate、排盘（作方法名时）

**from_input**:
从已校验的 `ZiweiInput` 走原始量捷径构造命盘（返回 `Ziwei`，非 `Result`）：生年干支已由调用方给出，安宫安星仍引擎算；命盘不具备真实出生年能力。
_Avoid_: 注入安星结果、快捷跳过规则、对已校验输入再包一层假错误通道

**真实出生年**:
调用方提供且可对应到具体历史时点的农历年序号；只有 `from_birth` 路径具备。生年干支只确定六十甲子位置，不能替代真实出生年生成绝对流年序号。
_Avoid_: 六十甲子代表年、从年柱反推历史年、把缺失年份写成 0

**农历年序号**:
调用方已消解的农历年整数；引擎用 `(year - 4) rem 10/12` 推导年干支，不做公历换算。
_Avoid_: 公历年、年柱字符串

**计算上下文**:
生成命盘时实际使用的完整标准化出生事实，随命盘保存。真实农历出生年已知时保存，未知时明确缺失；可用的时间查询能力由事实是否完整推导。不包含历法转换前的原始时间、转换过程或计算能力对象。
_Avoid_: 公历时间戳、时区、经纬度、真太阳时、历法转换记录、重复的能力开关

**核心排盘规则集标识**:
识别生成命盘时所用紫微排盘规则集的稳定领域标识，随计算上下文保存，用于解释与复现命盘。只标识排盘规则，不标识历法转换或命理分析规则。
_Avoid_: crate 版本、SDK 版本、Git commit、历法版本、分析规则版本

**ZiweiFly**:
单跳宫干飞化边：源宫支、四化象、目标宫支、被化星及自化标注。由本命宫干算出；自化出/入由排盘规则判定并随边保存。字段私有，只读 getter；外部不可伪造边及其自化标注（ADR-0010）。
_Avoid_: FlyEdge、多跳链、飞化串、断象叙述、公开字段字面量构造

**来因宫**:
由生年天干经固定表确定位置，叠落于十二宫之一，以唯一地支（`laiyin_branch`）表示。甲戌、乙酉、丙辰、丁未、戊午、己巳、庚辰、辛卯、壬寅、癸亥。
_Avoid_: 以「年干=宫干且非子丑」扫描十二宫（backup isLaiYin）作为 v1 权威

**Gender**:
命主性别，枚举为 `Yang`（阳/男）与 `Yin`（阴/女）。
_Avoid_: Male、Female

**大限顺逆**:
年干阴阳与 `Gender` 同性则顺行，异性则逆行（阳男阴女顺、阴男阳女逆）。甲丙戊庚壬为阳干。
_Avoid_: 与年支无关的另一套顺逆

**盘面坐标**:
规则内部用寅起环（0=寅）；对外用 `Branch` 与子起下标（0=子）。二者用固定公式互转，不得混用未标明的裸索引。
_Avoid_: 公共 API 上无标注的 0..=11 宫位整数

**身宫**:
由月、时安出的叠支，落在十二支之一，不是单独宫职。
_Avoid_: 第十三宫、可注入的身宫位

**太岁 / 流年命**:
流年命坐该农历年的年支；流年十二职以该支为命逆布。
_Avoid_: 与年支无关的流年命另算法（v1）

**宫干飞化**:
十二支本命宫干各飞四化形成的边集（≤48）。大限/流年视图不重算边，只换宫职到支的索引。
_Avoid_: 层干飞（若指第二套十二宫边）、重布宫干

**自化**:
宫干飞化边的固定标注：目标落在本宫为出，落在对宫为入，其余为无自化。由 `ziwei_core` 在排盘时判定并随 `Chart` 保存，不由查询层临时推导。
_Avoid_: 查询时再判断源宫与目标宫的几何关系

**ZiweiView**:
查询用的轻量视图：本命、第几步大限、或某农历年流年。只携带宫职叠落所需选择，不复制本命盘。大限/流年视图按叠落后的地支复用本命宫干飞化，不产生额外四化。大限/流年查询一律在 `Ziwei` 方法上显式传入本选择器。
_Avoid_: ChartView、整盘快照、可变的第二份 Ziwei、ZiweiHandle / with_view 薄句柄

**Palace / DecadeStep / DecadeYear**:
引擎产出的多字段结果类型；字段私有，对外只读 `const` getter，不可外部拼装（ADR-0010）。`PalaceRoleLabel` / `StarLabel` 等纯显示标签除外。
_Avoid_: 公开结果字段字面量、对显示标签做无意义封装
